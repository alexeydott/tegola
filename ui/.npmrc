# npm 11 project-scoped install-script allowlist (supply-chain hardening):
# deny-by-default; packages with install scripts must be approved explicitly.
# Only core-js has an install script in this tree (cosmetic donation postinstall);
# it is intentionally NOT approved - the build works without any install scripts.
allow-scripts=
